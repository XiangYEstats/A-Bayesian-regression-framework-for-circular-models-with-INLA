The "tutorial.pdf" is a brief instruction on how to fit a proposed circular regression model with INLA.
To run the code in "code.zip", set the R working dictionary to the folder "code".

The "wind_data.rds" is for downloading the data directly in R.
